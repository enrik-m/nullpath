# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.vercel/
*.local

# Derived from node_modules at predev/prebuild — see
# scripts/copy-sql-wasm.mjs. Don't commit; it'd duplicate the
# installed dep and go stale on every sql.js upgrade.
/public/sql-wasm.wasm

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Test + lint artifacts
coverage/
.vitest-cache/
.eslintcache
.prettiercache

# OS noise
Thumbs.db
ehthumbs.db
Desktop.ini

# Local secrets / signing material — never commit
.env
.env.local
.env.*.local
*.pfx
*.p12
*.key
*.keystore
